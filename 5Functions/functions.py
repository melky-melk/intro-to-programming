'''
1. Initialize a list of zeros e.g. [0]*5
2. We do a count controlled loop (since we know the number of iterations)
3. We get the number and chuck it into our list (use indexing!) 
'''
# initialising an empty list of 5 0's
ls = [0,0,0,0,0]

i = 0
# count controlled loop, where i increments until it hits 5
while (i < 5):
    # we are saving the user's input into the list. using i we can access what index we are at
    ls[i] = float(input(f"Number {i + 1}: "))

    i += 1

print("Your numbers were:")
# reset i to 0 again so we can do the same thing
i = 0

while (i < 5):
    print(ls[i])
    i += 1 
'''
1. Initialize a list of zeros e.g. [0]*5
2. We do a count controlled loop (since we know the number of iterations)
3. We get the number and chuck it into our list (use indexing!) 
'''
# initialising an empty list of 5 0's
ls = [0,0,0,0,0]

i = 0
# count controlled loop, where i increments until it hits 5
while (i < 5):
    # we are saving the user's input into the list. using i we can access what index we are at
    ls[i] = float(input(f"Number {i + 1}: "))

    i += 1

print("Your numbers were:")
# reset i to 0 again so we can do the same thing
i = 0

while (i < 5):
    print(ls[i])
    i += 1 
'''
1. Initialize a list of zeros e.g. [0]*5
2. We do a count controlled loop (since we know the number of iterations)
3. We get the number and chuck it into our list (use indexing!) 
'''
# initialising an empty list of 5 0's
ls = [0,0,0,0,0]

i = 0
# count controlled loop, where i increments until it hits 5
while (i < 5):
    # we are saving the user's input into the list. using i we can access what index we are at
    ls[i] = float(input(f"Number {i + 1}: "))

    i += 1

print("Your numbers were:")
# reset i to 0 again so we can do the same thing
i = 0

while (i < 5):
    print(ls[i])
    i += 1 
'''
1. Initialize a list of zeros e.g. [0]*5
2. We do a count controlled loop (since we know the number of iterations)
3. We get the number and chuck it into our list (use indexing!) 
'''
# initialising an empty list of 5 0's
ls = [0,0,0,0,0]

i = 0
# count controlled loop, where i increments until it hits 5
while (i < 5):
    # we are saving the user's input into the list. using i we can access what index we are at
    ls[i] = float(input(f"Number {i + 1}: "))

    i += 1

print("Your numbers were:")
# reset i to 0 again so we can do the same thing
i = 0

while (i < 5):
    print(ls[i])
    i += 1 
'''
1. Initialize a list of zeros e.g. [0]*5
2. We do a count controlled loop (since we know the number of iterations)
3. We get the number and chuck it into our list (use indexing!) 
'''
# initialising an empty list of 5 0's
ls = [0,0,0,0,0]

i = 0
# count controlled loop, where i increments until it hits 5
while (i < 5):
    # we are saving the user's input into the list. using i we can access what index we are at
    ls[i] = float(input(f"Number {i + 1}: "))

    i += 1

print("Your numbers were:")
# reset i to 0 again so we can do the same thing
i = 0

while (i < 5):
    print(ls[i])
    i += 1 
'''
1. Initialize a list of zeros e.g. [0]*5
2. We do a count controlled loop (since we know the number of iterations)
3. We get the number and chuck it into our list (use indexing!) 
'''
# initialising an empty list of 5 0's
ls = [0,0,0,0,0]

i = 0
# count controlled loop, where i increments until it hits 5
while (i < 5):
    # we are saving the user's input into the list. using i we can access what index we are at
    ls[i] = float(input(f"Number {i + 1}: "))

    i += 1

print("Your numbers were:")
# reset i to 0 again so we can do the same thing
i = 0

while (i < 5):
    print(ls[i])
    i += 1 
'''
1. Initialize a list of zeros e.g. [0]*5
2. We do a count controlled loop (since we know the number of iterations)
3. We get the number and chuck it into our list (use indexing!) 
'''
# initialising an empty list of 5 0's
ls = [0,0,0,0,0]

i = 0
# count controlled loop, where i increments until it hits 5
while (i < 5):
    # we are saving the user's input into the list. using i we can access what index we are at
    ls[i] = float(input(f"Number {i + 1}: "))

    i += 1

print("Your numbers were:")
# reset i to 0 again so we can do the same thing
i = 0

while (i < 5):
    print(ls[i])
    i += 1 
'''
1. Initialize a list of zeros e.g. [0]*5
2. We do a count controlled loop (since we know the number of iterations)
3. We get the number and chuck it into our list (use indexing!) 
'''
# initialising an empty list of 5 0's
ls = [0,0,0,0,0]

i = 0
# count controlled loop, where i increments until it hits 5
while (i < 5):
    # we are saving the user's input into the list. using i we can access what index we are at
    ls[i] = float(input(f"Number {i + 1}: "))

    i += 1

print("Your numbers were:")
# reset i to 0 again so we can do the same thing
i = 0

while (i < 5):
    print(ls[i])
    i += 1 
'''
1. Initialize a list of zeros e.g. [0]*5
2. We do a count controlled loop (since we know the number of iterations)
3. We get the number and chuck it into our list (use indexing!) 
'''
# initialising an empty list of 5 0's
ls = [0,0,0,0,0]

i = 0
# count controlled loop, where i increments until it hits 5
while (i < 5):
    # we are saving the user's input into the list. using i we can access what index we are at
    ls[i] = float(input(f"Number {i + 1}: "))

    i += 1

print("Your numbers were:")
# reset i to 0 again so we can do the same thing
i = 0

while (i < 5):
    print(ls[i])
    i += 1 
'''
1. Initialize a list of zeros e.g. [0]*5
2. We do a count controlled loop (since we know the number of iterations)
3. We get the number and chuck it into our list (use indexing!) 
'''
# initialising an empty list of 5 0's
ls = [0,0,0,0,0]

i = 0
# count controlled loop, where i increments until it hits 5
while (i < 5):
    # we are saving the user's input into the list. using i we can access what index we are at
    ls[i] = float(input(f"Number {i + 1}: "))

    i += 1

print("Your numbers were:")
# reset i to 0 again so we can do the same thing
i = 0

while (i < 5):
    print(ls[i])
    i += 1 
'''
1. Initialize a list of zeros e.g. [0]*5
2. We do a count controlled loop (since we know the number of iterations)
3. We get the number and chuck it into our list (use indexing!) 
'''
# initialising an empty list of 5 0's
ls = [0,0,0,0,0]

i = 0
# count controlled loop, where i increments until it hits 5
while (i < 5):
    # we are saving the user's input into the list. using i we can access what index we are at
    ls[i] = float(input(f"Number {i + 1}: "))

    i += 1

print("Your numbers were:")
# reset i to 0 again so we can do the same thing
i = 0

while (i < 5):
    print(ls[i])
    i += 1 
'''
1. Initialize a list of zeros e.g. [0]*5
2. We do a count controlled loop (since we know the number of iterations)
3. We get the number and chuck it into our list (use indexing!) 
'''
# initialising an empty list of 5 0's
ls = [0,0,0,0,0]

i = 0
# count controlled loop, where i increments until it hits 5
while (i < 5):
    # we are saving the user's input into the list. using i we can access what index we are at
    ls[i] = float(input(f"Number {i + 1}: "))

    i += 1

print("Your numbers were:")
# reset i to 0 again so we can do the same thing
i = 0

while (i < 5):
    print(ls[i])
    i += 1 
fndwjkfhihwuihfjkwhfhdjkfhdjhfdjfdfdef function_name(x, y):
    return x * y

multiplied = function_name(4, 5)
print(multiplied)
#return == return None
